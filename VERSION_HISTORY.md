# 版本记录

## v27 · 正式随机句子库与发布同步

- 将用户审核后的 61 条随机心意写入 `warmQuotes`，并新增可维护的数据快照。
- 保留「制作 → 藏进心意 → 手动切开 → 导出」主流程；切开是揭晓和导出的前置条件。
- 导出页精简为视频预览、进度、保存视频和发布图片笔记；作品在资源准备后自动收藏。
- 保留照片模式 1440 × 2560、文字／惊喜模式 1080 × 1920 的导出策略。
- 修复旧 WebView 兼容：预热流程不再依赖 `Promise.prototype.finally()`；照片 ID 在 `crypto` 不存在时安全降级。

## v26 · 制作页细节

- 空白蛋糕默认不显示奶油花边。
- 橡皮擦点击画布外空白后自动退出。
- 心意页只保留三种模式入口，提示胶囊置于输入框上方。

## v25 · 交互与性能

- 减少长按选择／复制干扰；抽屉外空白可收起抽屉。
- 新增导出进度与照片高分辨率策略。

## v24 · 主流程重构

- 确立制作、心意、切开、导出的核心体验。
- 接入图片笔记发布、视频系统分享和本机蛋糕柜。
